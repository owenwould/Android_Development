

 private  val clickListener = View.OnClickListener {view ->

        when (view.getId())
        {
          R.id.buttonCalc->
          {
              calculateWeight()
          }

        }
    }
